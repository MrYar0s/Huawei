CXX = g++
TARGET = test

.PHONY: all install clean uninstall

all: $(TARGET)
clean:
			rm -rf *.o
$(TARGET): source/test.cpp
			$(CXX) source/test.cpp -o test
install:
			install $(TARGET)
uninstall:
			rm -rf $(TARGET)
